<div class="container title">
  <div class="row">
    <h1 class="page-header text-center">
      Members <br>
      <small>Current roster of Vox Nova</small>
    </h1>
  </div>
</div>
<div class="container">
  <div class="row">
    <div class="col-xs-12">
      <h3>Current Members of Vox Nova</h3>
      <ul>
        <% @member.each do |person| %>
        <li><h5><a href="#<%= person.id %>" class=""><%= person.name %></a></h5></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
<div class="container">
  <div class="row">
  <div class="col-xs-12"><h3>Biographies</h3></div>
  </div>
</div>
<div class="container">
  <% @member.in_groups_of(3, false).each do |group| %>
  <div class="row row-flex row-flex-wrap">
    <% group.each do |item| %>
    <div class="col-sm-6 col-md-4 bioBox" id='<%= item.id %>'>
      <div class="thumbnail">
        <%= image_tag item.image.url(:large) %>
        <div class="caption">
          <h3 class="page-header" ><%= item.name %></h3>
          <p><%= item.bio.html_safe %></p>
        </div>
      </div>
    </div>  
    <% end %>
  </div>
  <% end %>
</div>
